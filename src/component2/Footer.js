import { Box } from '@mui/material';
import React from 'react';

function Footer(props) {
    return (
        <Box
            sx={{
                height: '7vh',
                textAlign: 'center',
                bgcolor: 'blue',
            }}
        >
            footer
        </Box>
    );
}

export default Footer;
